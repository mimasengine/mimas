# Toggle Audit — verdicts & cleanup plan (2026-07-16)

Generated from a 9-agent sweep (wf_aa6c2bf3-0e1): every pad chord / runtime sat_* flag /
compile switch, challenged against memories + docs + code comments + HW numbers. Dedup by name.
This file is the authoritative **consignation** — when a debate is closed, record it here.

**Counts (unique):** BAKE_REMOVE 16 · REMOVE 20 · KEEP_LIVE 19 · KEEP_PARKED 28 · RECORD_ONLY 111

## Execution progress (2026-07-16, all build-verified EXIT=0)

- ✅ **Plane-split → TAS-only** (`RP_DrawPlanesSplit` collapsed; removed `sat_plane_rowsplit`,
  `sat_plane_steal`, static-split + their slave bodies/cursors; **pad-C freed**; overlay `pm` baked).
  Restores the `WALL_ACC_MAX 160→144` pool cut row-split forced (still TODO to bump WALL_ACC_MAX back).
- ✅ **Freed 3 pad chords**: R+X (`sat_near_sprites` baked ON), R+Down (`rbg0_cell_koff` probe answered),
  R+Left (`rbg0_cell_nofb` probe answered) — and killed the `rbg0_reinit_force` gray-flash those caused.
- ✅ **Blit baked to c5**: the opt-in `slDMACopy` DMA path (HW-dead, B-bus-bound) removed; `W5` HUD-skip
  baked permanently ON; `blit_cfg[blit_mode]` reads inlined. (`blit_cfg` table left vestigial, harmless.)
- ⏳ **Remaining (follow-up, low-risk)**: RBG0 identity/dead var inlines (tiny — `rbg0_mode`/`rbg0_z_adj`/
  `rbg0_win_*` dead vars, `rbg0_tex_*`/`rbg0_pitch_adj` identity terms, dead macros); wallprep-slave
  harness removal. NOTE: `WALL_ACC_MAX` deliberately NOT bumped 144→160 (144 ≫ peak 57; keep the pool
  headroom the plane-split freed).
- ⏳ **Tier 4 (reserved — own validated pass)**: M1/M2/M3/M5 + `SAT_FLOOR_TEX` ftex removal — this one
  touches the LIVE render path (`sat_apply_mode` governs M4/M7) and shared `core/r_segs.c`, so it needs
  its own careful pass + HW validation, NOT a rushed bulk delete. `SAT_WALL_RMUL` bake has an open
  "validate seams on Ymir" TODO.

## BAKE_REMOVE — winner known, bake the value & delete the toggle

The default IS the winner; the A/B no longer informs anything. Bake the constant inline, delete the flag/chord/overlay. Many RBG0 entries are baked-to-IDENTITY (0 / 1.0x) → literally zero behaviour change.

| Toggle | How | Status | Frees | Verdict |
|---|---|---|---|---|
| `W5 (blit_cfg[].w5 static-HUD-skip)` | runtime:blit_cfg[blit_mode].w5=1 (permanently  | SETTLED_SHIPPED | No chord; trivial .text (constant-folds three w5 conditional | HW-CONFIRMED win: c-=10.0ms vs c5=8.7ms 1p idle standstill (-1.3..-1.5ms = 14% = exactly the 32 HUD rows), committed as the default. It is the winner and permanently ON. Bake the .w5 reads to constant 1 (the three `!blit_cfg[blit_mode].w5`  |
| `blit_mode (blit_cfg[] selector)` | runtime:blit_mode=1 (c5; fixed, no longer pad- | SETTLED_SHIPPED | No pad chord (L+A already reused for sat_cd_persistent, dg_s | Winner c5 (CPU+W5) has been the fixed default since the L+A ring was cut 2026-07-07. blit_mode is `static int = 1` and is NEVER written at runtime, so blit_cfg[blit_mode] is a compile-constant {dma:0,w5:1}. Bake dma=0/w5=1 inline, delete th |
| `SAT_WALL_RMUL (reciprocal-multiply wal` | compile:SAT_WALL_RMUL=1 | SETTLED_SHIPPED | nothing (compile #if); removes the WDIV division-fallback so | Default ON; the SH-2-has-no-hardware-divide win is not contested and the wall-flush cost is a known bottleneck. Bake the rmul path and delete the OFF divisions. FLAG: the code carries a 'pixel-validate the seams on Ymir' TODO — confirm seam |
| `sat_ftex_mode (floor-tile emit mode 0-` | runtime:sat_ftex_mode=3 (resolver-set; pad cyc | SETTLED_CUT | pad:Y freed long ago; ~0 chord now; small .text/.bss with th | The pad-Y 0-5 cycle is already cut and the FTX overlay row is (void)-ed; the value is inert in every reachable mode (sat_vdp1_floor=0). Bake it out with the SAT_FLOOR_TEX decision (keep as resolver-set only if M5 survives). |
| `sat_ftex_slave (F-build on slave SH-2)` | runtime:sat_ftex_slave=1 (frozen; L+Y A/B reas | SETTLED_CUT | L+Y already reassigned to ceiling SQ; a few .bss bytes | The L+Y slave-F-build A/B was cut and sl1 kept as the compile default; only executes in parked ftex modes. Bake =1 (or delete with the whole ftex path). No live decision rides on it. |
| `sat_plane_tas` | pad:C (pm-cycle pm1) ; runtime:sat_plane_tas=1 | SETTLED_SHIPPED | pad C-alone chord (shared with the whole pm cycle); collapsi | Winner is known and universal (never regresses). The pad-C 3-way cycle only exists to A/B against two settled losers (static, row-split). Bake sat_plane_tas=1, delete the cycle, free the C chord. Senior-caveat: memory's 2026-06-29 note said |
| `rbg0_mode` | runtime:rbg0_mode=0 (pad Y — REASSIGNED away) | SETTLED_DEAD | ~4B .data + dead 'rbg0_mode==1/!=0' branches at @6315,@6320, | Grep-proven NEVER assigned (no 'rbg0_mode =' anywhere) — constant 0. Pad Y was reassigned to SAT_FLOOR_PERFSIM / FLOOR-SQ, so the 3-mode cycle it documents is gone. Inline as 0: 'rbg0_mode==0' is always true, 'rbg0_mode==1' (software-floor  |
| `rbg0_pitch_adj` | runtime:rbg0_pitch_adj=0x100 (pad R+Up/Down on | SETTLED_SHIPPED | ~4B .data | Baked to 0x100 ('+0x100 found'); total 1p pitch = RBG0_PITCH(0x300)+0x100 = 0x400, HW-validated. Written only under dead RBG0_TUNE_PAD. Fold into #define RBG0_PITCH 0x400 for the 1p (non-split) branch @2205 and delete the variable. |
| `rbg0_split_scroll` | runtime:rbg0_split_scroll=16 (pad C+Up/Down un | SETTLED_SHIPPED | ~4B .data + two per-frame 64-bit muls (@2208-2209) | IDENTITY: 16 = 1.0x, so ((int64_t)(-viewx)*16)>>4 == -viewx. The comment even says 'with the right pitch/sd it lands at 1.0'. Bake to plain -viewx/-viewy; removes two 64-bit multiplies per split frame for zero change. |
| `rbg0_split_yaw` | runtime:rbg0_split_yaw=0 (pad C+Left/Right und | SETTLED_SHIPPED | ~4B .data + one add in the per-frame slRotZ | IDENTITY: baked to 0 ('0 once the projection is correct' — the old 4864 compensated a since-fixed restore bug). The '(sat_split_p1hw ? rbg0_split_yaw : 0)' term @2207 is always +0. Delete the variable and the term; zero behavior change. |
| `rbg0_split_yawsc` | runtime:rbg0_split_yawsc=16 (pad C+Up/Down und | SETTLED_SHIPPED | ~4B .data + one per-frame mul/shift (@2206) | IDENTITY: '16 = 1.0x = correct per disasm; the x2 was from the old Cy=112+pitch band-aid, not the yaw'. (viewangle*16)>>4 == viewangle. Bake to the plain (int)(viewangle>>16); zero change. |
| `rbg0_tex_orient` | runtime:rbg0_tex_orient=5 (pad L+C only under  | SETTLED_SHIPPED | ~4B .data + collapses the 8-way switch in both upload loops  | Baked to 5 = mirrorV, 'found on HW', HW-validated shipping 1p floor. Written only under RBG0_TUNE_PAD=0 (dead). Hardcode mirrorV (v=63-v style) and delete the switch/variable; zero behavior change, real .text/loop simplification. |
| `rbg0_tex_xoff` | runtime:rbg0_tex_xoff=0 (pad L+Left/Right only | SETTLED_SHIPPED | ~4B .data + one add per texel in the upload loops | IDENTITY: baked to 0, so (x+0)&63 == x&63. Written only under dead RBG0_TUNE_PAD. Drop the '+rbg0_tex_xoff' from @2302/@2369/@2399; zero change. |
| `rbg0_tex_yoff` | runtime:rbg0_tex_yoff=0 (pad L+Up/Down only un | SETTLED_SHIPPED | ~4B .data + one add per texel in the upload loops | IDENTITY: baked to 0. Same as rbg0_tex_xoff — drop '+rbg0_tex_yoff' from @2303/@2369/@2400; zero change. |
| `rbg0_z_adj` | runtime:rbg0_z_adj=0 (pad R+Left/Right only un | SETTLED_DEAD | ~4B .data + one add in the per-frame slTranslate (@2210) | Written ONLY under RBG0_TUNE_PAD=0 (@7181-7182), so it is permanently 0; the '+rbg0_z_adj' @2210 is always +0. Delete the variable and the term; zero change. |
| `sat_near_sprites` | pad:R+X \| runtime:sat_near_sprites=1 (default | SETTLED_SHIPPED | pad R+X + the extern plumbing + row-7 'ns' overlay field; ~t | Standard FastDoom cull, shipped default-ON, perf settled, tradeoff is cosmetic-only and the owner already accepted it. Bake =1 inline and free the R+X chord (chords are scarce). CAVEAT: the 610u threshold (magic const 40000000, per-axis asy |

## REMOVE — dead code, delete it

Measured net-loss / no-op / superseded. Delete the branch + body + any chord.

| Toggle | How | Status | Frees | Verdict |
|---|---|---|---|---|
| `DMA blit path (blit_cfg[].dma / slDMAC` | runtime:blit_cfg[blit_mode].dma=0 (unreachable | SETTLED_DEAD | ~2 blit_cfg rows (dma=1) + the dead slDMACopy branch .text | HW-confirmed NO win (b/MST/fps UNCHANGED on the c->d flip -> the blit is B-bus write-bandwidth-bound, not CPU-bound), and async is CATEGORICALLY HW-DEAD (SCU->VDP2 B-bus write freezes the console, 4x F00001, documented SEGA hardware law; sl |
| `M1_FULL (full VDP1 floor+ceiling tile ` | runtime:sat_m=M1 (parked, off the pad-Z cycle) | SETTLED_DEAD | no chord (off-cycle); enables the SAT_FLOOR_TEX bake (severa | Net-negative on real HW (M4 +44% vs M1) and explicitly closed: 'M1/M2/M3 are now REMOVED from the live Z-cycle ... the A/B is closed, do not re-propose'. The regression-anchor role is obsolete (refactor shipped + HW-validated). Delete the e |
| `M2_FLOORS (VDP1 leftover-floors only)` | runtime:sat_m=M2 (parked, off-cycle) | SETTLED_DEAD | no chord; part of the SAT_FLOOR_TEX bake | Same settled-negative floor-tile deport as M1; off-cycle, dead. The VDP1 floor-tile fill is memory-bound F-build, not fill-bound, so the deport can't win. Delete with M1/M3. |
| `M3_CEILS (VDP1 ceilings only)` | runtime:sat_m=M3 (parked, off-cycle) | SETTLED_DEAD | no chord; part of the SAT_FLOOR_TEX bake | A cheap VDP1 CEILING deport was proven STRUCTURALLY DEAD (layer order inverted: NBG1 prio6 > VDP1 5 > RBG0 4; a coarse quad either hides under NBG1 or bleeds over sky). Off-cycle, dead. Delete with M1/M2. |
| `SAT_FLOOR_PERFSIM + floor_perfsim_mode` | compile:SAT_FLOOR_PERFSIM=1 + runtime:floor_pe | SETTLED_DEAD | no chord (its pad-Y binding is shadowed by SAT_FLOOR_TEX's S | Fully dead given SAT_FLOOR_TEX=1: the only writer of floor_perfsim_mode is behind '#elif SAT_FLOOR_PERFSIM' which never compiles, so the var is stuck at 0, the PERFSIM overlay row is (void)-cut, and its profiler-reset key field never change |
| `SAT_SPR_PRIO7_TEST (prio-7 mechanism p` | compile:SAT_SPR_PRIO7_TEST=0 | SETTLED_CUT | nothing (compile #if, default 0); small .text/source scaffol | A deliberately 'wrong-looking' yes/no mechanism proof, superseded by the shipped per-command prio path (SAT_WPN_VDP1/WORLD_THINGS). Its job is done — delete the scaffold and its #elif branches. |
| `VDP1_FLOOR_TEST (index-0-skip floor pr` | compile:VDP1_FLOOR_TEST=1 | SETTLED_CUT | no chord; small .text (the perfsim hook + index-0 skip diagn | A validation-era probe for the now-settled floor deport; only meaningful in parked modes / the dead perfsim path. Fold into the SAT_FLOOR_TEX=0 cut. |
| `sat_ftex_blit_overlap (blit hides in F` | runtime:sat_ftex_blit_overlap=0 (frozen off; R | SETTLED_DEAD | R+Z already off the ring; a few .bss bytes + ftex_overlap_fl | Marginal and cut per the refactor memory; present-coherence-sensitive and only bites in parked ftex modes. Delete the flag + ftex_overlap_flip plumbing. |
| `sat_vdp1_floor (core: consult the floo` | runtime:sat_vdp1_floor (apply_mode-owned; =0 i | SETTLED_DEAD | no chord; removes above-ceiling wall-clamp branches in core  | Always 0 in M0/M4/M6/M7; the only writers that set it are parked M1/M2/M3 and the DEAD perfsim path (:7269, uncompiled under SAT_FLOOR_TEX=1). Its core-side branches (sat_wall_cross_hi mirrors) are dead weight. Delete with the floor-deport  |
| `sat_vdp1_floor_claim / sat_vdp1_ceil_c` | runtime:both=1 (apply_mode-owned; nonzero only | SETTLED_DEAD | no chord; part of the SAT_FLOOR_TEX bake | Pure plumbing for the parked M1/M2/M3 floor-tile deport; always effectively unused in the reachable cycle. Delete with the ftex path. |
| `sat_plane_rowsplit` | pad:C (pm-cycle pm2) ; runtime:sat_plane_rowsp | SETTLED_DEAD | part of the pad-C cycle; deletes rp_plane_rows_body + R_Draw | Not a baseline, not parked-for-future: it is a proven net loss whose one theoretical advantage (dominant-plane balance) is fatally undercut by walk duplication and already handled elsewhere. Pure dead code — remove branch + body. Its .text  |
| `sat_plane_steal` | pad:Y (compiled OUT: SAT_DIAG_SLAVE_TOGGLES=0) | SETTLED_DEAD | no LIVE chord (pad-Y already compiled out via SAT_DIAG_SLAVE | TAS FULLY supersedes it (same purpose, atomic, HW-proven better). Keeping both parked mechanisms is redundant — TAS is the revivable 'work-steal'. The inferior two-pointer path is genuinely dead. Note: RANK3_WALLPREP.md:7 calling sat_plane_ |
| `RBG0_CELL_KOFF_PROBE` | compile:RBG0_CELL_KOFF_PROBE=0 | SETTLED_DEAD | the #define (dead once koff baked to K_ON) | Explicitly 'temporary, 2026-07-07'. The probe resolved (4bpp fix). Delete alongside rbg0_cell_koff. |
| `RBG0_COMMIT_VIA_SLSYNCH` | compile:RBG0_COMMIT_VIA_SLSYNCH=1 | SETTLED_DEAD | the #define + its 8-line comment (dead) | Repo-wide grep: referenced ONLY at its own #define — never used in any #if. The ship path is the manual block-flush with NO slSynch (doc: 'No slSynch is used on the bitmap path'). Vestigial macro; delete it and the misleading comment. |
| `VDP2_SKY_FORCE_CYC` | compile:VDP2_SKY_FORCE_CYC=0 | SETTLED_DEAD | ~small .text (sky_cell_force_cyc body + its #if sites @6339) | Dead experiment: 'Did NOT change the sky-shows-floor HW bug -> gated OFF'. The bug it targeted no longer exists on HW. Memory explicitly lists it as a dead experiment to distrust. Delete the flag + sky_cell_force_cyc(). |
| `rbg0_cell_koff` | pad:R+Down (runtime:rbg0_cell_koff=RBG0_CELL_K | SETTLED_DEAD | pad R+Down (~1 chord) + rbg0_cell_koff/reinit_force plumbing | Probe's question is answered: 16-color (4bpp) cells give 2 reads/dot WITH perspective K_ON, coexisting with the framebuffer snow-free — no need to drop the coefficient. Bake koff=0 (K_ON) into the cell path and free R+Down. |
| `rbg0_cell_nofb` | pad:R+Left (runtime:rbg0_cell_nofb=0) | SETTLED_DEAD | pad R+Left (~1 chord) + the nbg1_bit override @6324 | Probe's question is answered: 4bpp cells are snow-free WITH the framebuffer ON, so 'drop the framebuffer' is no longer a live hypothesis. Purely destructive diag (blanks HUD/weapon). Bake nofb=0 and free R+Left. |
| `rbg0_win_cx / rbg0_win_cy / rbg0_win_d` | runtime:rbg0_win_cx=80,rbg0_win_cy=80,rbg0_win | SETTLED_DEAD | ~12B .data + 3 dead statics | Superseded by the slDispCenterR/slSetScreenDist split path (rbg0_split_cx/cy/sd). Grep-proven: referenced ONLY at their declarations plus one stale comment @7188 — never read by any transform. Pure dead variables; delete them and the stale  |
| `sat_wallprep_defer` | runtime:sat_wallprep_defer=0 (tied: dg_saturn. | SETTLED_DEAD | no own pad; reclaims the RP_QueueWall snapshot path + walljo | Exists solely to feed the dead wall-prep-slave path; with that path removed the deferred-queue substrate has no purpose. Remove together with sat_wallprep_slave. (Keep only if the owner elects to park the whole harness behind SAT_DIAG_SLAVE |
| `sat_wallprep_slave` | pad:L+R (compile-gated behind SAT_DIAG_SLAVE_T | SETTLED_DEAD | pad L+R (already compile-freed); reclaims the whole harness  | Debate is definitively CLOSED (3x HW negative; the 2nd SH-2 only pays for compute-bound cache-warm work, generalizes). The pad chord is already gated out but the machinery is still LINKED dead weight (sat_wallprep_slave is a mutable extern  |

## KEEP_LIVE — genuine open A/B, DO NOT clear

Still actively deciding an open question (HW-unvalidated feature, open config, or safety hatch).

| Toggle | How | Status | Frees | Verdict |
|---|---|---|---|---|
| `SAT_DRP_CART_STAGE` | compile:SAT_DRP_CART_STAGE=1 (in-source #defin | OPEN | nothing (compile flag, no chord; inert without a 4MB cart) | Default-ON is the intended winner, but it is HW-UNVALIDATED because it only engages with a real 4MB RAM cart (drp_stage_to_cart returns early unless sat_cart_usable>=4MB; on ODE/no-cart it is fully inert). The comment explicitly frames it a |
| `M0_SOFT (all-software reference)` | pad:Z (cycle position) | SETTLED_SHIPPED | nothing | Deliberately kept as the ground-truth reference ('software mode IS the A/B reference'). Costs only a cycle step + a few apply_mode branches. Keep. |
| `M4_RBG0 (default mode)` | pad:Z (cycle position) / runtime:sat_m=M4 | SETTLED_SHIPPED | nothing | The winner and boot default; it is the anchor of the cycle, not a removable A/B. Keep as-is. |
| `M6_NOSPR (world-things-software A/B)` | pad:Z (cycle position) / runtime:sat_things_hw | SETTLED_SHIPPED | nothing | The runtime A/B that measures VDP1 world-things cost; world-things is a HORDE lever dormant in shareware but the endgame goal is big/crowded WADs, where M6 is the measuring tool. Cheap (no chord). Keep as the sprite-only reference. |
| `M7_LOWRES` | pad:Z (cycle position) / runtime:sat_lowres | OPEN | nothing | A genuinely-shipped selectable mode with an OPEN config verdict: 1p/2p win (+8-18%), 3p/4p neutral; user explicitly 'on garde le mode' and 'on continue d'explorer'. Keep both the mode and the A/B. |
| `sat_m master axis + pad-Z cycle` | pad:Z | OPEN | nothing | The single most-used dev lever and the only way to reach the M0/M6/M7 A/B references. M4 is the winner but M4<->M7 and M4<->M0/M6 are still actively cycled on HW. Keep. |
| `sat_wall_clamp / SAT_WALL_CLAMP (VDP1 ` | pad:R+A / compile:SAT_WALL_CLAMP=1 (also force | OPEN | pad:R+A if ever removed | Phase-1 shipped+merged, but the R+A on/off is a genuine live A/B (the wall-clamp Bp cost vs far-wall famine tradeoff is still tuned, and it interacts with M5). Borderline — likely belongs to the 'VDP1 walls' sibling subsystem; keep live and |
| `sat_mark_suppress` | pad:L+B ; runtime:sat_mark_suppress auto = (sa | OPEN | nothing — keep the L+B chord | The only genuinely OPEN lever in this subsystem. The 3/4p default is settled (that half could be baked), but the 2p case is a live undecided A/B and L+B is the tool that decides it. Do NOT clear. Revisit for BAKE_REMOVE once the 2p verdict  |
| `RBG0_SPLIT_P1HW` | compile:RBG0_SPLIT_P1HW=1 | OPEN | nothing (auto-activates on 2p + M!=soft) | Compiled ON and active in 2p, but the split floor is baked only on E1M1/Ymir and HW-UNVALIDATED. The gate is settled-on but the underlying feature quality is open; keep the flag as the clean escape hatch until an HW pass confirms the reproj |
| `VDP2_SPLIT_HW_SKY` | compile:VDP2_SPLIT_HW_SKY=1 (runtime toggle hw | OPEN | if removed: pad L+C + the nbg0_sky_window_apply machinery | Feature is compiled in and default-ON but NOT yet validated on real Saturn (cosmetic size/anchor WIP; no-slSynch window path is the Ymir<->HW divergence zone). Genuine open A/B — keep both the compile gate and its runtime toggle until an HW |
| `hwsky_split_on` | pad:L+C (runtime:hwsky_split_on=1) | OPEN | if baked: pad L+C (~1 chord) | The load-bearing live toggle for VDP2_SPLIT_HW_SKY. HW-unvalidated feature that jumps at toggle (different parallax anchor); this chord is exactly how the user will A/B it on real HW. Do not bake until validated. |
| `sat_aimd_damp` | pad:L+X (cycles 0/1/2) \| runtime:sat_aimd_dam | OPEN | nothing recommended; keep L+X | The only genuinely OPEN lever in scope: mode 2 (wbudget) is an unresolved experiment and even the shipped mode 1 is admitted un-validated (INERT on E1M1, needs combat). Keep L+X live. Cheap simplification available: mode 0 (plain) is the kn |
| `sat_clear_slave` | pad:R+C \| runtime:sat_clear_slave=1 (default  | SETTLED_SHIPPED | would free pad R+C + a small handler if baked; NOT recommend | The PERF A/B is closed (ship ON), so this is not a live perf decision — but keep R+C as a HW-only cache-coherency SAFETY escape hatch. dual-SH2 cache races are the highest-risk, Ymir-invisible bug class; the one-flip disambiguator 'is this  |
| `sat_split_balance` | pad:L+Right (runtime:sat_split_balance=0 defau | OPEN | nothing (keep chord) | Freshly shipped 2026-07-15, UNCOMMITTED, HW-PENDING; it is a core piece of the current 3/4p perf/quality exploration (the replacement for the dead M7-low-res 3/4p lever). Requires the wall_acc[].pot per-view capture to work. User is activel |
| `sat_split_thingcull` | pad:L+Left (runtime:sat_split_thingcull=1 defa | SETTLED_SHIPPED | L+Left chord + ~40B .text (only when baked post-HW) | Flipped DEFAULT-ON 2026-07-15; the definition comment itself states it is 'impractical to A/B on HW' (needs a monster-dense room, can't stand still to read the overlay) and thresholds are conservative -> the A/B question is essentially sett |
| `sq_ceil (+ sq_ceil_view[4] split mirro` | pad:L+Y | OPEN | nothing (keep chord) | Biggest single software-cost lever in the subsystem: HW datum ceiling FLAT = P 58.4->29.3ms in 1p M7 (~half of P), P 29->11 in M4. Kept deliberately independent of floor because LD is ~invisible on the ceiling but matters on the floor. Stil |
| `sq_floor (+ sq_floor_view[4] split mir` | pad:Y | OPEN | nothing (keep chord) | Active 3/4p tuning: 3/4p now BOOTS fxxf (floor+ceil FLAT) because RBG0 is off and LD is a no-op under M7 detailshift; the per-count fxxf default is uncommitted/HW-pending and the pad still lets the user dial back to fll/full live. Genuine q |
| `sq_wall (+ sq_wall_view[4] split mirro` | pad:R+Y | OPEN | nothing (keep chord) | Both a 1p quality knob and the SINGLE most important active 3/4p perf lever: memory m7-lowres-fill-bound-not-34p calls split BAND/FLAT walls 'the biggest 3/4p target (Bp ~14ms x4)' after M7 low-res was proven useless there. Split-SQ shipped |
| `sat_wall_clamp / SAT_WALL_CLAMP (pad R` | pad:R+A=toggle \| compile:SAT_WALL_CLAMP=1 (de | SETTLED_SHIPPED | if later baked: pad R+A + a few lines .text (toggle + row-6  | Winner is ON (SHIPPED+MERGED to master 2026-07-04, default via SAT_WALL_CLAMP=1). I keep the R+A A/B rather than bake it because this is a Z-ORDER-CORRECTNESS lever (memory documents multiple painter bugs exposed only when the clamp moved s |

## KEEP_PARKED — dead but kept as a documented harness

| Toggle | Why parked (not deleted) |
|---|---|
| `VDP1_MANUAL_CHANGE (present-sync; gate` | Out-of-subsystem (this is the VDP1 present-tearing group's lever) — listed only because =0 already compiles out the L+Z/R+Z chords, which are the 'leftover pad chords in the input handler' this audit  |
| `sat_ftex_blit_overlap (blit<->F-build ` | The perf question (does overlapping the blit with the slave F-build reclaim ~6ms?) is genuinely UNRESOLVED — never validated on HW — and the upside is large (master otherwise idles in RP_AuxWait ~17-2 |
| `SAT_KEEP_REJECT` | Drop-REJECT-in-streaming is the settled winner: the numsectors^2/8 matrix is 45-125KB of PU_LEVEL on big Doom II maps, the largest late alloc and a fragmentation-OOM magnet, for only an O(1) sight ear |
| `sat_cd_persistent` | Measured HW REGRESSION on the only console tested (Phoebe SD ODE): loads 2->4s / 6->7s + constant game-time dilation on BOTH p0 and p1, because holding a GFS handle open keeps read-ahead-to-EOF churni |
| `sat_sight_maxdist` | Behaviour-CHANGING (monsters go blind past N units -> AI + demo-desync risk), default 0/off, never adopted or validated -- it is an opt-in harness, not an active A/B. It frees no chord and costs one i |
| `M5_CONVEX (convex-exact floor quads)` | The ONLY documented future VDP1-deport path (low-ROI, floor-heavy scenes only). It shares SAT_FLOOR_TEX with the dead M1-M3, so it is the sole reason to keep that machinery. FLAG for the owner: if M5  |
| `SAT_FLOOR_TEX (floor-texture-on-VDP1 s` | Its shipped purpose (M1-M3 deport) is SETTLED_DEAD, but it is the machinery behind the still-parked M5 convex-exact idea + the 'ftex fxdiv->FixedDiv' micro-opt. KEEP_PARKED for now; if the owner aband |
| `SAT_WORLD_THINGS_VDP1 (world sprites o` | Default ON, shipped. The runtime A/B is M6 (things software). Bare #if guard = no binary cost for OFF; keep as compile fallback. Bake only in a cleanliness pass (it guards a lot of thing-cache code th |
| `SAT_WPN_VDP1 (player weapon on VDP1 pr` | Default ON, shipped + HW-validated. The runtime A/B already exists (M0 forces the software weapon via sat_wall_skip). As a bare #if guard it adds no binary cost, so keep it as a harmless compile fallb |
| `VDP1_MANUAL_CHANGE (present-sync gate;` | Adjacent (VDP1 present/tearing subsystem, not modes) but relevant here because at =0 it frees the Z-modifier chords, leaving pad-Z clean for the mode cycle. Owner explicitly parked present-sync 'for g |
| `RBG0_CELL_4BPP` | The correct/settled choice within the (parked) cell floor — 4bpp is the snow fix. The 256c #else is a compile-time fallback with no runtime cost. Keep at 1 as part of the parked cell harness. |
| `RBG0_DEBUG_ONTOP` | Pure bring-up diagnostic, off. Cheap #if harness worth keeping for the next RBG0-visibility bug; the priority order it flips (documented in memory part5-hw-sky-split) is a recurring source of confusio |
| `RBG0_DISPLAY` | Snow-source bring-up diag, permanently on. Cheap #if harness; baking removes a trivial term. Low value either way — keep as documented harness. |
| `RBG0_LINECOL_TEST` | Compiled in but visually inert (mode 0 -> slColRateLNCL forced to 0 @2527). Kept for the RUNG-C distance-gradient rework AND — subtly load-bearing — its W0 CCAL window arming is what lets VDP2_SPLIT_H |
| `RBG0_SPLIT_TUNE` | Parked tuning harness for the HW-unvalidated 2p split floor. Because the split floor still needs a real-HW pass, keep this and the split knobs it drives rather than hard-baking them. |
| `RBG0_TUNE_PAD` | Parked; the found 1p values (mirrorV, yoff 0, pitch +0x100) are the baked defaults. Cheap harness to re-tune the 1p floor if a flat mis-aligns. Keep off. (Its baked knobs are separately BAKE_REMOVE ca |
| `rbg0_kind / rbg0_kind_want` | Cell floor is PAUSED (clean+snow-free on HW but has undiagnosed HW triangle artifacts). Deliberately kept as the harness toward the endgame RPB dual-param floor (RPA floor + RPB ceiling -> drop NBG1). |
| `rbg0_lc_trans / rbg0_linecol_ratio` | Only consumed when rbg0_linecol_mode!=0 (parked), so inert today. Note: the comment @2433 also lists 'zonek/zoneoff' veil params that DO NOT EXIST as variables — stale comment, worth trimming. Keep th |
| `rbg0_linecol_mode` | The parked gradient's master switch, 0. Pad handlers for it were removed for the bake-only ship; kept for the future RUNG-C session. Keep as documented WIP harness. |
| `rbg0_split_cx` | Baked non-identity (80) used @2221. Keep parked pending HW validation of the split floor. |
| `rbg0_split_cy` | Baked non-identity (80) used @2221 (currently equal to rbg0_split_cx=80). Keep parked pending HW validation. |
| `rbg0_split_hz` | Baked-from-Ymir value, live-used @6387. Non-identity (80), so can't be trivially folded away. Since the 2p HW split floor is HW-unvalidated, keep it as a parked static behind RBG0_SPLIT_TUNE rather th |
| `rbg0_split_pitch` | Baked non-identity value used @2205. Same HW-unvalidated caveat as rbg0_split_hz — keep parked, revisit when the split projection gets a real-HW pass. |
| `rbg0_split_sd` | Baked non-identity (7/16) used @2222. Keep parked pending HW validation. |
| `SAT_DIAG_SLAVE_TOGGLES` | Correctly parks the dead pad handlers so they cost nothing in the ship build. Keep as the documented revive-switch — UNLESS sat_wallprep_slave is fully deleted (recommended), in which case the L+R hal |
| `VDP1_MANUAL_CHANGE (master compile-gat` | Owner verdict 2026-07-02 at the define itself: 'present-sync abandoned for good ... seam is structural ... code kept behind this guard for a possible future VDP1-walls revisit; flip to 1 to restore.'  |
| `vdp1_couple_nbg1 (pad R+Z: defer NBG1 ` | Secondary lever of the parked present subsystem; entirely under `#if VDP1_MANUAL_CHANGE`(=0), compiled out, no chord consumed. Follows the master gate — nothing to clear independently. |
| `vdp1_present_manual (pad L+Z: 1-cycle-` | Winner is the shipping auto present (boot default 0). The whole L+Z handler sits inside `#if VDP1_MANUAL_CHANGE` (=0) so it is compiled out — no pad L+Z is consumed today (Z with L held falls through  |
