# Boot matrix -- 2026-08-26 02:24

Generated by `tools/boot_matrix.ps1`. **This is a BUILD + PRE-FLIGHT matrix, not a boot test**:
nothing here proves a disc reaches the title screen. The launch checklist at the bottom is the
human half, and it is not optional before a `core/` commit.

Pool floor: **4.8 KB** (below it, SGL's boot allocation returns null and the game loops).
Elapsed: 517.6 min.

| WAD | build | pool KB | bin bytes | cue | note |
|---|---|---|---|---|---|
| Doom2 | ok | 28,00 | 114 925 776 | ok | Doom II |
| grid1212 | FAIL |  |  | missing | 1994 visplane-overflow demo -- sizes VP_POOL_PLANES, watch vp<peak>.<ovf> |
| HR | FAIL |  |  | missing | Hell Revealed -- ds/o pressure |
| SCYTHE | ok | 28,00 | 16 678 032 | ok | MAP30 763 KB / MAP29 684 KB -- zone exhaustion, the last hard I_Error |

## The human half -- launch, in this order

For each row that built: reach the title screen, start map 1, read row 11 `LIM`.
Stop at the first failure -- the list is ordered so a break shows up early.

```powershell
powershell -File run_ymir.ps1 -Wad Doom2
powershell -File run_ymir.ps1 -Wad SCYTHE
```

Named heavy cases worth a warp beyond map 1: **Doom2 MAP13** (lazy texture directories),
**Tnt MAP20** (the 1p tic-bound ledger spot), **SCYTHE MAP30** (zone exhaustion),
**grid1212** (visplane pool -- read `vp<peak>.<ovf>`, the second digit must stay 0).
